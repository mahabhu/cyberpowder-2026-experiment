# Slicing config moved into gnb_rf_b200_tdd_n78_20mhz.yml under
# cu_cp.amf.supported_tracking_areas[].plmn_list[].tai_slice_support_list[].
# This file is kept as a no-op so the existing services/srs-gnb.service unit's
# `-c /var/tmp/etc/srsran/slicing.yml` argument still resolves.
#
# (srsRAN's parser accepts a comments-only YAML file without complaint.)
